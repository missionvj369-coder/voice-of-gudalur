# Force npm to include devDependencies during install (needed for vite build)
# NODE_ENV is set to production in netlify.toml [build.environment] for the
# runtime, but NPM_FLAGS="--include=dev" ensures build tools are installed
include=dev
