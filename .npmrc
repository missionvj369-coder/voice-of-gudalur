# Force a clean install on Netlify (no stale cache from previous failed builds)
CI=false
# Install devDependencies — vite is a devDependency and needed for the build step
NODE_ENV=development
# Don't use the deprecated 'prepare' lifecycle hooks in prod
ignore-scripts=false
